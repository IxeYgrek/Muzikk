# Screenshots

This folder is a fallback. Prefer dropping captures in
`Images/Screenshot` at the project root — that folder is scanned first.

Number the files (`01-home.png`, `02-search.png`, …) to control the order.
Accepted extensions: `jpg`, `jpeg`, `png`, `webp`, `avif`, `gif`.
